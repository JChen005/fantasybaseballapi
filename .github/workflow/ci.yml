name: Player API CI

on:
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - main

concurrency:
  group: player-api-ci-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: 20
  API_PATH: player-api

jobs:
  player-api:
    name: Player API tests and app check
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ${{ env.API_PATH }}

    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: ${{ env.API_PATH }}/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Verify Player API app loads
        run: node -e "require('./src/app'); console.log('Player API app loaded successfully')"